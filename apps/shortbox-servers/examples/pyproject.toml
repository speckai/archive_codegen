[project]
name = "shortbox-examples"
version = "0.1.0"
description = "Examples for the shortbox service"
authors = [{ name = "Speck", email = "support@speck.sh" }]
dependencies = [
  "shortbox",
  "pydantic>=2.10.6",
  "python-dotenv>=1.0.1",
  "requests>=2.32.3",
  "typer>=0.15.1",
]
requires-python = ">=3.12"

[tool.uv.sources]
shortbox = { path = "../../../packages/shortbox" }
